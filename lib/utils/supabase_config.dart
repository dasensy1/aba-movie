class SupabaseConfig {
  // Keep only public client credentials here if you add Supabase later.
  // Never place sb_secret/service-role keys inside the Flutter app.
  static const String url = '';
  static const String anonKey = '';
}
